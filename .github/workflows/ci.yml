name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Fetch historical v0.8.0 rollback baseline
        env:
          BASELINE_REPOSITORY: https://github.com/EddieTYP/image-prompt-library.git
          BASELINE_TAG: v0.8.0
          BASELINE_SHA: 527d1486fbb929c951952ff4b770d092171e6a5c
        run: |
          if ! git rev-parse --verify "refs/tags/$BASELINE_TAG^{commit}" >/dev/null 2>&1; then
            git fetch --no-tags "$BASELINE_REPOSITORY" "refs/tags/$BASELINE_TAG:refs/tags/$BASELINE_TAG"
          fi
          test "$(git rev-parse "refs/tags/$BASELINE_TAG^{commit}")" = "$BASELINE_SHA"
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[dev]'
      - name: Install frontend dependencies
        run: npm install
      - name: Run tests
        run: python -m pytest -q
      - name: Run frontend behavior tests
        run: npm run test:frontend
      - name: Build local app
        run: npm run build
      - name: Build read-only demo
        run: npm run build:demo

  windows-installer:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Install Python dependencies
        run: python -m pip install -e '.[dev]'
      - name: Install frontend dependencies
        run: npm install
      - name: Build local app
        run: npm run build
      - name: Run Windows installer contracts
        run: python -m pytest -p no:cacheprovider -q tests/test_windows_installer.py tests/test_library_archives.py
      - name: Run native Windows installer smoke
        shell: powershell
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/windows-installer-smoke.ps1

  macos-backup-restore:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Install Python dependencies
        run: python -m pip install -e '.[dev]'
      - name: Run portable backup and restore contracts
        run: python -m pytest -p no:cacheprovider -q tests/test_library_archives.py
